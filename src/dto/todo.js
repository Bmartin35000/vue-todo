export class todoDto {
    constructor(id, title, completed, created_at) {
        this.id = id;
        this.title = title;
        this.completed = completed;
        this.created_at = created_at;
    }
}